# Customer analytics

Status: anonymous waiting/service session logic and aggregation are **implemented**; real-scene quality is **unmeasured**.

Temporary customer IDs start waiting on confirmed waiting-zone entry. A stable worker interaction can start service. Departure ends the session or records left-without-service. Unconfirmed metrics are reported unavailable; customer face images are never intentionally saved.

