# Reporting

Status: local CSV and manager HTML generation are **implemented**. Google Sheets upload is **optional**.

Reports aggregate completed interval events, never individual frames. Confirmed, possible, unknown, absence, break and camera-unavailable time remain distinct. A Google failure does not invalidate successful local files. Low observation coverage shows a warning. Manager review preserves the original automated result.

